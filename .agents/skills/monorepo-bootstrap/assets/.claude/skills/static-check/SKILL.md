---
name: static-check
description: 検証ゲートのコマンドを一括実行し、成否を表形式で報告する
---

# /static-check

正本は `docs/harness/skills/static-check.md`。これを読み、記載の手順どおり実行する。
プロジェクト固有値は本ディレクトリの `references/` 配下 profile を参照する（存在する場合のみ）。
