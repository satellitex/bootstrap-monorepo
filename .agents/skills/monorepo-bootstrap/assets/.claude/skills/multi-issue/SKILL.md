---
name: multi-issue
description: 複数 issue を Planner–Worker のエージェントスウォームで並列実装し、issue ごとに PR を作成する
---

# /multi-issue

正本は `docs/harness/skills/multi-issue.md`。これを読み、記載の手順どおり実行する。
プロジェクト固有値は本ディレクトリの `references/` 配下 profile を参照する（存在する場合のみ）。
