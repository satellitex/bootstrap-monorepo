---
name: security-audit
description: auditor / verifier の 2 wave でリポジトリを多次元セキュリティ監査し、レポート PR 作成と issue 起票まで自律実行する
---

# /security-audit

正本は `docs/harness/skills/security-audit.md`。これを読み、記載の手順どおり実行する。
プロジェクト固有値は本ディレクトリの `references/` 配下 profile を参照する（存在する場合のみ）。
