---
name: code-sync
description: 対象拡張子のソースコメントを origin/main から抽出し、「現状の事実のみ」3 原則違反と doc-style コメント内の内部参照を検出して修正 PR にまとめる
---

# /code-sync

正本は `docs/harness/skills/code-sync.md`。これを読み、記載の手順どおり実行する。
プロジェクト固有値は本ディレクトリの `references/` 配下 profile を参照する（存在する場合のみ）。
