---
name: deploy-verify
description: 最新取り込みから deploy・検証までを一気通貫で実行し、各ステップの成否を明示判定して失敗時は issue を起票する
---

# /deploy-verify

正本は `docs/harness/skills/deploy-verify.md`。これを読み、記載の手順どおり実行する。
プロジェクト固有値は本ディレクトリの `references/` 配下 profile を参照する（存在する場合のみ）。
