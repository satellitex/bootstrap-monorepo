<!--
Issue に対応するときに使用する計画・記録テンプレート。
各セクションを埋めて `$ISSUE_DIR/task-note.md` として出力する（運用規約は docs/issues/README.md）。

- 適用対象:
  - `/multi-issue`（worker が issue ごとに実体化する）
  - メインエージェント判断（バグ修正・リファクタ・小規模変更・その他。
    `#<issue> 対応して` 等の直接指示を含む）

記入タイミング:
- 着手時に §0〜§4 / §5.1 / §6 / §7 を埋める（作業計画はこの task-note から導出する）
- 完了時に §5.2〜§5.4（検証結果 / traceability / リファクタ）と §8（PR / ADR リンク）を確定する
- それ以外の実装中の更新はしない（方針・スコープが変わった場合のみ上書き更新する）

該当しないセクションは削除せず「なし」と明記する（記入漏れと区別するため）。
-->

# Task Note — Issue #{{番号}}: {{1 行要約}}

> {{このタスクで達成するゴール状態を 1 行で}}

## 0. メタ情報

| 項目 | 値 |
|------|-----|
| Issue | [#{{番号}}](https://github.com/{{GITHUB_ORG}}/{{REPO_NAME}}/issues/{{番号}}) |
| Labels | {{ラベル一覧、なければ「なし」}} |
| Branch | `{{作業ブランチ名}}` |
| フロー | {{`/multi-issue` / メインエージェント判断}} |
| フロー判定理由 | {{なぜそのフローを選んだか。例: 「単一ファイルのバグ修正でメインエージェント判断が適切」}} |
| 依存 | {{依存 Issue / PR、なければ「なし」}} |

---

## 1. 背景：何を解くか

| 項目 | 内容 |
|------|------|
| 現状 | {{今どうなっているか（観測できた事実のみ）}} |
| 期待 | {{どうなっているべきか}} |
| 影響 | {{放置した場合の影響・誰が困るか}} |

---

## 2. 調査結果・原因

{{タスク種別に応じて記述する。推測と確認済みの事実を区別すること。}}

- **バグ修正**: 再現条件 / 根本原因 / 原因箇所（`path/to/file.ts:123` 形式）
- **リファクタ**: 現状構造の問題 / `docs/styles/refactoring_guide.md` の該当観点
- **ハーネス開発**: 既存ハーネスの現状 / 変更が必要な理由
- **その他**: 調査で判明した事実と、その確認方法（実行したコマンド・読んだファイル）

| # | 確認した事実 | 確認方法 |
|---|------------|---------|
| F1 | {{事実 1}} | {{コマンド / ファイルパス / ログ}} |
| F2 | {{事実 2}} | {{確認方法}} |

---

## 3. 対応方針

| ID | 判断 | Why | 却下した代替案と理由 |
|----|------|-----|-------------------|
| **D1** | {{採る手 1}} | {{§2 のどの事実に基づくか}} | {{代替案 / 却下理由。なければ「なし」}} |
| **D2** | {{採る手 2}} | {{理由}} | {{なし}} |

> D-ID のうち後続タスクに影響する設計判断は `/create-adr` で ADR 化し、§8 にリンクする。

---

## 4. 変更対象

| 種類 | パス | 変更概要 |
|------|------|---------|
| 新規 | {{パス}} | {{役割}} |
| 変更 | {{パス}} | {{何をどう変えるか（対応する D-ID）}} |
| 削除 | ~~{{パス}}~~ | {{削除理由（D-ID）}} |

---

## 5. 検証

### 5.1 テスト方針

| 観点 | 内容 |
|------|------|
| 追加/更新するテスト | {{テストファイルとケース。バグ修正なら再現テストを先に書く（Red → Green）}} |
| 既存テストの扱い | {{リファクタなら「既存テスト全 PASS を維持」等}} |
| 手動検証 | {{自動テスト化できない場合の手順と根拠。不要なら「なし」}} |

### 5.2 実行結果（完了時に記入）

検証コマンドの定義は `docs/harness/skills/shared/verification-gates.md` が正本。

| コマンド | 結果 |
|---------|------|
| `pnpm run test`（または対象パッケージの test） | {{PASS / 件数}} |
| `pnpm run lint` / `pnpm run typecheck` / `pnpm run format:check` | {{PASS}} |
| `pnpm run build` | {{PASS}} |
| {{その他（`/static-check`・E2E smoke 等）}} | {{結果}} |

### 5.3 traceability 成果物（opt-in:traceability 採用時のみ。テストを追加・変更・リネーム・削除した場合）

| 項目 | 内容 |
|------|------|
| `$ISSUE_DIR/traceability-matrix.yaml` | {{新規作成 / 更新 / 不要（理由）}} |
| `$ISSUE_DIR/traceability.md` | {{新規作成 / 更新 / 不要（理由）}} |
| 他 Issue の matrix repoint | {{repoint した matrix パス、または「不要（テスト名の変更なし）」}} |

> レポートの雛形 → `docs/issues/templates/traceability.md`。opt-in:traceability を採用していない場合は「対象外（traceability 不採用）」と明記する。

### 5.4 リファクタパス（PR 前に実施）

| 項目 | 内容 |
|------|------|
| 実施内容 | {{`/simplify` 実行 / `docs/styles/refactoring_guide.md` の観点で自己チェック / 差分が小さくリファクタ対象なし}} |
| 適用した修正 | {{重複の統合・関数抽出・不要な抽象の除去など。なければ「なし」}} |
| 振る舞い不変の確認 | {{リファクタ後に再実行したテストと結果}} |
| コミット分離 | {{`refactor:` コミットのハッシュ、または「変更なし」}} |

> 手順の正本 → `docs/styles/team-feedback/refactor-before-pr.md`

---

## 6. スコープ

### In scope

- {{今回対応する範囲 1}}

### Out of scope

- {{対応しない範囲 1}}: {{理由}} / {{別 Issue 起票の有無（`#<番号>` または「起票不要」）}}

> scope 外の指摘は本 PR で対応せず、既存 Issue 追記か新規 Issue で追跡する
> （`docs/styles/team-feedback/scope-boundary.md`）。

---

## 7. リスクと影響範囲

| Risk | 影響 | 緩和 |
|------|------|------|
| R1: {{リスク 1}} | {{影響範囲（呼び出し元・既存データ・運用等）}} | {{緩和策 / 対応する D-ID}} |

---

## 8. Related

| 種別 | 参照先 | 関係 |
|------|--------|------|
| Issue | #{{番号}} | 起票元 |
| PR | #{{番号}} | 本対応 |
| ADR | `docs/adr/ADR-{{...}}.md` | {{§3 のどの D-ID を永続化したか。なければ「なし」}} |
| 関連成果物 | `docs/issues/{{...}}/` | {{関連する他 Issue の成果物}} |
| 参考資料 | {{URL / ファイルパス}} | {{参照理由}} |
