# audit

> この文書は `docs/audit/`（セキュリティ監査レポート置き場）の命名規約である。監査の実施手順・観点は `docs/harness/skills/security-audit.md` に委譲し、ここには書かない。

- `/security-audit` の統合レポートは本ディレクトリに `YYYY-MM-DD_SECURITY_REPORT.md` の命名で出力する。
- 同日の再実行は既存レポートを更新する（別名ファイルを増やさない）。
