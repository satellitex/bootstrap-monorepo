# Architecture — {{PRODUCT_NAME}}

> この文書は {{PRODUCT_NAME}} の内部設計正本（システム境界・主要コンポーネント・データ配置）である。
> 個別ファイル・テスト・マイグレーション・タスク単位の実装履歴は本文に含めない。ユースケース別の動的フロー（シーケンス図）は専用文書へ委譲し、本書からリンクする。

## 抽象度規約

- 本書は「現状の構成」だけを書く。変更の経緯・判断理由は ADR（`docs/adr/`）と git 履歴に委ね、本文に実装履歴を書かない。
- コンポーネント粒度で記述し、個別ファイル名・関数名を書かない。
- 動的フロー（API 利用シーケンス等）は別文書に分離し、本書は静的構造（境界・コンポーネント・データ配置）に限定する。

## High-Level Overview

（システム全体の構成図と、設計上の大きな分離原則をここに記述する。図は ASCII / mermaid / 画像のいずれでもよい）

## 開発スコープと責任分界

| 領域 | 開発主体 | 対応コンポーネント |
|------|----------|-------------------|
| | | |

## 論理コンポーネント

| コンポーネント | 責務 | 主な利用者 |
|----------------|------|------------|
| | | |

## API 境界

（呼び出し元 → パス prefix → 提供コンポーネントの対応をここに記述する）

## データ配置

（どのデータをどこに保持するか、公開 / 非公開の境界、SSOT とキャッシュの区別をここに記述する）
